apiVersion: v1
kind: Service
metadata:
  name: agentic-rag-mcp
  labels:
    app: agentic-rag-mcp
spec:
  type: ClusterIP
  selector:
    app: agentic-rag-mcp
  ports:
    - name: mcp-http
      port: 8000
      targetPort: mcp-http
